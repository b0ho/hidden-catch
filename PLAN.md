# 크레이지아케이드 "히든캐치" 스타일 틀린그림찾기 모바일 웹 게임

## Context

크레이지아케이드의 "히든캐치" 미니게임(클릭으로 캐릭터를 이동시켜 두 그림 사이의 다른 부분을 찾아내는 방식)을 모티브로, 앱이 아닌 **반응형 웹 페이지**(모바일 브라우저 + 데스크톱 브라우저 겸용, PWA로 "홈 화면에 추가"까지 지원)로 구현한다.

기존에 이런 게임/캔버스 프로젝트는 없어 신규 프로젝트로 시작하되(`/Users/b0ho/git` 내 새 디렉토리), 사용자가 다른 프로젝트(`glimpse` admin)에서 반복적으로 쓰는 **Vite + React 19 + TypeScript + Tailwind CSS** 스택을 그대로 재사용해 친숙하게 맞춘다. 스테이지 콘텐츠 제작(AI 호출·이미지 편집)은 배포되는 런타임이 아니라 로컬 Node.js 스크립트(`tools/`)로 분리한다(아래 아키텍처 참고) — 이후 리더보드 등으로 런타임 서버가 필요해지면 `happydasol`의 **Express + better-sqlite3** 패턴을 참고해 추가하면 된다.

핵심 난제는 "AI로 틀린 영역 5개를 만든다"는 요구사항이다. 이미지 생성 AI에게 "비슷하지만 5곳만 다른 그림 두 장"을 통째로 만들게 하면 그림 전체가 달라져 좌표 판정이 불가능해진다. 사용자와 논의 후, **하이브리드 방식**(vision AI가 편집할 5개 영역의 좌표·편집 방식을 지정 → 스크립트가 그 좌표에 실제 픽셀 편집을 적용)으로 확정했다. 이렇게 하면 정답 좌표를 정확히 알 수 있어 "캐릭터가 도달했는지" 판정이 가능해진다. 게임 모드는 싱글 플레이어(실시간 대전 없음)다.

**스테이지는 실시간 생성이 아니라 사전 제작(pre-built) 방식으로 간다.** 처음에는 샘플 3개 스테이지만 만들고, 게임 플레이 자체가 어느 정도 완성된 뒤에 스테이지를 계속 추가해나가는 순서로 진행한다. 이미지 소스도 단계적으로 붙인다: 1단계(지금)는 구글 등에서 수동으로 받은 무료 이미지 3장으로 테스트하고, Unsplash/Pixabay API 연동은 게임이 완성 단계에 가까워졌을 때 붙인다. 이 순서 덕분에 초반에는 API 키 발급이나 라이브 서버 없이도 바로 게임 플레이 완성도에 집중할 수 있다.

캐릭터는 크레이지아케이드 캐릭터처럼 **귀엽고 동글동글하며 입체감(볼륨감)이 있는 디자인**이어야 한다(아래 "캐릭터 비주얼 설계" 참고).

## 핵심 아키텍처

```
hidden-catch/
├── client/          # Vite + React 19 + TypeScript + Tailwind CSS (정적 사이트, 배포에 서버 불필요)
│   ├── public/
│   │   ├── stages/
│   │   │   ├── stage-01/{original.jpg, modified.jpg, meta.json}
│   │   │   ├── stage-02/{original.jpg, modified.jpg, meta.json}
│   │   │   └── stage-03/{original.jpg, modified.jpg, meta.json}
│   │   └── manifest.json              # PWA manifest (홈 화면 추가 지원)
│   └── src/
│       ├── pages/StageSelect.tsx      # 스테이지 목록 선택 화면 (처음엔 3개, 계속 늘어남)
│       ├── pages/Stage.tsx            # 게임 플레이 화면 (레이아웃 방향 결정 포함)
│       ├── game/ImagePanel.tsx        # 이미지 1장 + 캐릭터 오버레이 + diff 하이라이트를 담는 컴포넌트
│       ├── game/Character.tsx         # 동글동글 입체감 캐릭터 (CSS/SVG 기반)
│       ├── game/useCharacterMovement.ts  # 클릭 좌표(%) 계산 → 이동 애니메이션 → 도착 판정
│       ├── game/hitTest.ts            # 퍼센트 좌표-반경 기반 정답 판정
│       ├── hooks/useOrientation.ts    # 뷰포트 방향(세로/가로) + 브레이크포인트 판별
│       └── data/stages.ts             # 정적 스테이지 목록 메타(3개 → 계속 추가), stage-*/meta.json을 참조
├── tools/           # 개발자가 로컬에서만 실행하는 "스테이지 제작" 스크립트 — 배포 대상 아님
│   ├── generate-stage.ts              # 소스 이미지 → diffPlanner → imageEditor → client/public/stages/에 결과물 저장
│   ├── source-images/                 # 1단계: 구글 등에서 수동으로 받은 무료 이미지 원본 보관
│   ├── services/diffPlanner.ts        # Claude vision 호출 → 5개 편집 영역 JSON 제안
│   ├── services/imageEditor.ts        # sharp 기반 실제 픽셀 편집 (5가지 편집 함수)
│   └── services/imageSource.ts        # 2단계에 추가: Unsplash/Pixabay API로 소스 이미지 자동 검색
├── .env                                # ANTHROPIC_API_KEY (tools 스크립트 실행 시에만 사용, 로컬 전용)
└── shared/types.ts  # StagePayload, DiffRegion 등 공용 타입
```

`tools/`는 게임을 플레이할 때 실행되는 코드가 아니라, **개발자가 새 스테이지를 만들 때 한 번 실행하는 콘텐츠 제작 파이프라인**이다(`npm run generate-stage -- --image tools/source-images/beach.jpg --id stage-04`). 실행 결과물(원본/편집 이미지 + 정답 좌표 `meta.json`)은 정적 파일로 `client/public/stages/`에 저장되고, 실제 게임은 이 정적 파일만 읽는다. 그래서 배포 시 런타임 서버가 필요 없고 Vite로 빌드한 정적 사이트만으로 웹 배포가 가능하다 — Claude/이미지 API 키가 필요한 작업은 전부 로컬 스크립트 실행 시점에서 끝나기 때문에 브라우저에 노출될 걱정도 없다. (추후 리더보드나 사용자가 직접 트리거하는 실시간 스테이지 생성이 필요해지면 그때 별도 런타임 서버 도입을 재검토한다.)

### 게임엔진(Phaser 등) 도입 여부 검토

**결론: 별도 게임엔진 없이 React + CSS(DOM 기반)로 구현한다.** 검토한 근거:

- 이 게임의 실제 요구사항은 "이미지 위에서 캐릭터 1개가 클릭 지점으로 걸어가고, 원형 히트 영역 도달 여부를 판정"하는 정도다. 물리엔진, 다중 스프라이트 충돌, 타일맵, 실시간 네트워킹처럼 Phaser/PixiJS가 강점을 갖는 영역이 전혀 필요 없다.
- **오히려 이번에 추가된 반응형 레이아웃 요구사항(모바일 세로=상하 스택, 가로/데스크톱=좌우 배치)이 게임엔진 도입에 불리한 이유가 된다.** Phaser 같은 엔진은 자체 캔버스 좌표계와 씬 시스템으로 화면을 관리하기 때문에, 방향 전환 시 패널을 상하↔좌우로 재배치하는 것을 CSS만큼 자연스럽게 처리하지 못하고 캔버스 리사이즈/좌표 재계산 로직을 별도로 짜야 한다. React+CSS는 Tailwind의 반응형/방향 유틸리티(`flex-col`/`flex-row`, `orientation` 미디어쿼리)로 이 문제를 그대로 해결한다.
- 좌표는 픽셀이 아니라 **이미지 컨테이너 기준 백분율(0~100%)**로 다루면, 패널이 상하로 쌓이든 좌우로 나란히 서든, 화면 크기가 바뀌든 diff 좌표·캐릭터 위치·클릭 판정 로직이 전혀 달라지지 않는다. 캔버스 기반이었다면 레이아웃이 바뀔 때마다 캔버스 크기/DPI/좌표 변환을 다시 계산해야 했을 것이다.
- 캐릭터 이동 애니메이션은 CSS `transform: translate(x%, y%)` + `transition`으로 충분히 부드럽게 구현 가능하고("찾음" 이펙트도 CSS 애니메이션/Framer Motion으로 처리), 별도 게임 루프(`requestAnimationFrame`)나 에셋 프리로더 같은 엔진 인프라가 필요 없다.
- 부가 이점: 장르 선택 화면, HUD(점수/타이머), 스테이지 클리어 모달 등 나머지 UI는 이미 React로 만들 수밖에 없는데, 게임 영역만 별도 엔진(캔버스)로 만들면 React state와 엔진 내부 상태를 동기화하는 계층이 추가로 필요해진다. 전부 React 컴포넌트로 두면 이 동기화 비용이 사라진다.

향후 캐릭터가 여러 명이 되거나 실시간 대전 모드가 추가되는 등 요구사항이 커지면 그때 PixiJS(react-pixi) 같은 경량 렌더러 도입을 재검토하면 된다.

## 게임 메커니즘 설계

1. **스테이지 선택**: `StageSelect.tsx`에서 `data/stages.ts`에 정의된 스테이지 목록(처음엔 3개)을 썸네일 카드로 보여주고, 선택 시 해당 스테이지의 정적 파일(`/stages/stage-0N/*`)을 로드.
2. **스테이지 제작 파이프라인** (`tools/generate-stage.ts`, 개발자가 로컬에서 실행 — 플레이 중에는 호출되지 않음):
   - **1단계(지금)**: `tools/source-images/`에 구글 등에서 수동으로 받은 무료 이미지를 넣고 경로를 직접 지정해서 실행
   - **2단계(게임 완성도 어느정도 확보 후)**: `imageSource.ts`를 통해 Unsplash/Pixabay에서 장르 키워드로 이미지 자동 검색하도록 확장 — 이때부터 스테이지 목록에 "장르"도 함께 추가
   - Claude(vision)에 소스 이미지를 전달해 "편집 가능한 작고 명확한 5개 영역"을 중심좌표(0~1 비율)·반경·편집종류(색상변경/객체제거(블러)/크기변경/좌우반전/복제) JSON으로 응답받음
   - 응답을 스키마 검증 후, `imageEditor`가 sharp로 해당 좌표에 실제 편집을 적용해 "다른 이미지"를 생성
   - 결과물(`original.jpg`, `modified.jpg`, `meta.json`: `{ diffs: [{x, y, radius}, ...] }`)을 `client/public/stages/stage-0N/`에 저장하고 `data/stages.ts`에 등록
3. **플레이 화면**: 원본/편집본 두 `ImagePanel`을 나란히 또는 위아래로 배치(아래 반응형 레이아웃 규칙 참고). 캐릭터는 편집본 패널 위에서 클릭·탭한 좌표(퍼센트)로 걸어서 이동(CSS transform 애니메이션). 도착 지점이 특정 diff의 반경 내면 "발견" 처리(하이라이트 이펙트 + 카운트 증가), 5개 모두 찾으면 스테이지 클리어.
4. **판정 로직**: MVP는 런타임 서버가 없으므로 `meta.json`의 diff 좌표를 클라이언트에서 직접 비교해 판정한다(정답 좌표가 devtools에 노출되는 것은 알려진 한계로 남겨둠). 이후 리더보드/부정행위 방지가 필요해지면 그때 판정용 런타임 서버를 별도로 추가한다.

## 캐릭터 비주얼 설계

크레이지아케이드 캐릭터 특유의 **둥글둥글하고 말랑말랑하면서 입체감 있는(볼륨감 있는) 느낌**을 게임엔진/스프라이트 시트 없이 순수 CSS/SVG로 구현한다.

- `Character.tsx`는 단일 SVG(또는 레이어드 `div` + `border-radius: 50%`) 컴포넌트로 제작: 큰 원형 몸통 + 짧은 팔다리 + 크고 동그란 눈으로 구성한 심플한 마스코트 형태.
- **입체감**은 그라디언트와 그림자로 표현: 몸통에 `radial-gradient`로 상단 하이라이트(광택) + 하단 음영을 넣고, 바닥에는 캐릭터 발밑 `box-shadow`(타원형 그림자)를 깔아 공중에 붕 뜬 느낌이 아니라 바닥에 붙어있는 느낌을 준다.
- **생동감**: 대기 상태에서는 미세한 숨쉬기/깜빡임 CSS 애니메이션(`scale` 미세 반복), 이동 중에는 걷는 방향으로 살짝 기울이고 통통 튀는(squash & stretch) 바운스 애니메이션을 `transition`/`keyframes`로 적용. 별도 스프라이트 프레임 이미지 없이 벡터 기반이라 어느 해상도(모바일 확대 등)에서도 깨지지 않는다.
- 색상은 CSS 변수로 빼두어 나중에 캐릭터 스킨(색상 변형)을 스테이지/플레이어별로 쉽게 바꿀 수 있게 설계.
- 이 방식은 위 "게임엔진 도입 여부 검토"에서 내린 DOM 기반 결론과도 맞물린다 — 별도 렌더러 없이 React 컴포넌트 하나로 귀엽고 입체감 있는 캐릭터를 완성할 수 있다.

## 반응형 레이아웃 규칙

`useOrientation` 훅이 `window.matchMedia('(orientation: portrait)')`와 뷰포트 너비(Tailwind `md` 브레이크포인트 기준 데스크톱 판별)를 조합해 아래 3가지 상태 중 하나를 결정하고, `Stage.tsx`에서 두 `ImagePanel`을 감싼 컨테이너의 `flex-direction`을 그에 맞게 전환한다.

| 상황 | 배치 |
|---|---|
| 모바일 + 세로모드(portrait) | 위/아래 스택 (`flex-col`) |
| 모바일 + 가로모드(landscape) | 좌우 배치 (`flex-row`) |
| 데스크톱(웹 기본, 화면폭 충분) | 좌우 배치 (`flex-row`) 고정 |

이미지·diff·캐릭터 좌표를 모두 퍼센트 기반으로 다루기 때문에(위 게임엔진 검토 참고) 방향 전환 시 좌표 재계산 없이 CSS `flex-direction`만 바꿔주면 되고, 각 `ImagePanel`은 `aspect-ratio`를 유지해 이미지가 찌그러지지 않게 한다.

## PWA (웹/앱 겸용)

네이티브 앱을 따로 만들지 않고 `manifest.json` + 서비스 워커로 PWA를 구성해 "홈 화면에 추가" 시 앱처럼 전체화면으로 동작하게 한다. 반응형 Tailwind 레이아웃으로 모바일 브라우저와 데스크톱 브라우저 모두 대응.

## 마일스톤

**1단계 — 핵심 게임 완성 (샘플 3스테이지, 수동 이미지)**
1. 프로젝트 스캐폴딩: Vite+React+TS+Tailwind 클라이언트, `tools/` 스크립트 골격, `shared/types.ts`
2. `tools/services/diffPlanner.ts`(Claude vision으로 5개 편집 영역 제안) + `imageEditor.ts`(sharp 편집 함수 5종) 구현
3. `tools/generate-stage.ts`로 구글에서 수동으로 받은 이미지 3장을 소스로 스테이지 3개(`stage-01`~`03`) 생성, 육안 검수
4. 캐릭터 비주얼(`Character.tsx`, CSS/SVG 기반 동글동글 입체감 디자인 + 대기/이동 애니메이션)
5. 게임 화면(DOM 기반): `ImagePanel`, `useCharacterMovement`, `hitTest`, 스테이지 클리어 플로우
6. 반응형 레이아웃(`useOrientation` 기반 상하/좌우 전환) + PWA manifest 적용
7. `StageSelect.tsx`에서 3개 스테이지 목록 노출, end-to-end 플레이 가능한 상태로 완성

**2단계 — 콘텐츠 확장 (게임이 어느 정도 완성된 뒤)**
8. `tools/services/imageSource.ts`로 Unsplash/Pixabay API 연동, 장르 키워드 기반 자동 이미지 소싱으로 전환
9. 스테이지 개수 확대 및 장르 분류 추가, 난이도별 diff 개수/반경 조절, 최고기록 저장 등

## 사전 준비 사항

- Anthropic API 키 (`tools/generate-stage.ts` 실행 시 Claude vision 호출용, 로컬 `.env`에만 필요)
- 1단계: 구글 등에서 라이선스 확인된 무료 이미지 3장을 준비해 `tools/source-images/`에 배치 (Unsplash/Pixabay API 키는 2단계까지 불필요)
- **초기 설정**: `/Users/b0ho/git` 자체는 git 저장소가 아니며 `glimpse` 등 여러 무관한 프로젝트가 함께 있는 컨테이너 디렉토리이므로, 거기에 바로 `git init`하지 않는다. 대신 `/Users/b0ho/git/hidden-catch/` 새 폴더를 만들고 그 폴더 안에서만 `git init`을 실행해 다른 프로젝트와 완전히 분리된 저장소로 시작한다(구현 착수 시 가장 먼저 수행).
- 프로젝트 폴더명은 `hidden-catch`로 가정 (원하면 변경 가능)

## 검증 방법

- 스테이지 생성: `tools/generate-stage.ts`를 3개의 소스 이미지에 대해 실행해 `client/public/stages/stage-0N/`에 원본/편집 이미지와 `meta.json`이 정상 생성되는지 확인, 편집본을 원본과 육안 비교해 정확히 5곳만 달라졌는지 검수
- 클라이언트: `npm run dev`로 로컬 구동 후 브라우저(데스크톱/모바일 뷰포트 both)에서 스테이지 선택 → 로드 → 클릭 시 캐릭터 이동 애니메이션 → 정답 좌표 클릭 시 발견 이펙트 → 5개 모두 찾으면 클리어 화면까지 end-to-end로 직접 플레이해서 확인 (3개 스테이지 모두)
- 캐릭터 비주얼: 대기 애니메이션(숨쉬기/깜빡임)과 이동 중 바운스 애니메이션이 크레이지아케이드 느낌의 둥글고 입체감 있는 모습으로 보이는지 직접 확인
- 모바일 대응: Chrome 개발자도구 모바일 에뮬레이션 및 실기기(또는 반응형 미리보기)로 터치 조작 확인
- 레이아웃 전환: 개발자도구에서 모바일 세로(상하 스택) ↔ 가로 회전(좌우 배치) ↔ 데스크톱 폭(좌우 배치) 세 가지 상태를 모두 전환해보며 이미지 비율 유지 및 캐릭터/diff 좌표가 어긋나지 않는지 확인
