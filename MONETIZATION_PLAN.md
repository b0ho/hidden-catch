# 히든캐치 수익화 계획

## 목표와 원칙

지금까지 만든 5개 카테고리(한옥/사찰/들꽃/소품/절벽 동굴)는 계속 무료로 유지한다. 이미 즐기고 있는 유저를 갑자기 유료화하면 반발이 크므로, **수익화는 "새로 추가되는 것"에 붙인다** — 새 힌트, 새 꾸미기, 새 카테고리. 기존 경험은 그대로 두고 그 위에 선택적으로 얹는 구조다.

3개 층을 순서대로 쌓는다: 광고(마찰 없음) → 소액 결제(헤비 유저) → 콘텐츠 확장 판매(장기 매출). 구독은 사용자 수가 쌓인 뒤 나중에 검토한다.

## 공통 전제: 최소 백엔드

세 층 모두 "클라이언트가 스스로 신고하는 상태"를 서버가 저장/검증해야 의미가 있다 (안 그러면 devtools로 다 무료가 됨). 별도 논의였던 리더보드용 백엔드와 합쳐서 하나로 간다.

- **스택**: Express + better-sqlite3 (파일 기반 DB, 별도 DB 서버 불필요 — `happydasol` 프로젝트 패턴 재사용)
- **플레이어 식별**: 로그인 없이 클라이언트에서 생성한 UUID를 localStorage에 저장 → 요청마다 헤더/바디로 전송. 닉네임은 선택 입력. 나중에 진짜 로그인이 필요해지면 이 UUID에 연결하면 됨
- **최소 테이블 설계**:
  ```sql
  players (id TEXT PK, nickname TEXT, created_at)
  clears (player_id, stage_id, time_left, created_at)          -- 리더보드용 (기존 논의)
  purchases (player_id, product_id, provider, provider_txn_id, amount, created_at)
  entitlements (player_id, key TEXT, value TEXT)                 -- 'ad_free', 'hint_pack:5', 'skin:blue', 'category:diving-helmet' 등
  ```
- 클라이언트의 `lib/records.ts`는 지금처럼 localStorage를 1차 캐시로 유지하고, 백엔드가 꺼져 있어도(배포 전 단계 등) 로컬 전용으로 계속 동작하게 만든다 — 정적 사이트 배포 옵션을 잃지 않기 위함.

## 1층 — 보상형 광고 (서버 불필요, 가장 먼저 착수 가능)

**무엇을 파는가**: 힌트가 떨어졌을 때 "광고 보고 힌트 +1".

- **기술적 제약**: 이 게임은 네이티브 앱이 아니라 PWA/모바일 웹이라, AdMob 같은 성숙한 보상형 광고 SDK를 그대로 못 쓴다. 웹에서 보상형 광고를 붙이는 선택지:
  - **Google Ad Manager (소규모 계정) 의 rewarded web ad** — 정석이지만 심사/설정이 까다로움
  - **서드파티 웹 보상형 광고 네트워크** (Adsterra, PropellerAds 등) — 심사가 상대적으로 쉬워 인디 프로젝트에서 흔히 씀. 다만 광고 품질/단가는 AdMob보다 낮음
  - **장기적으로는 Capacitor 등으로 앱스토어에 올려 정식 AdMob을 붙이는 쪽이 안정적/수익성 둘 다 좋음** — 지금 당장보다는 유저 수가 어느 정도 쌓인 뒤 재검토
- **UX**: 힌트 버튼(`🔍0`)이 비활성화되는 대신, "📺 광고 보고 +1" 버튼으로 바뀜. 일시정지 화면에도 동일 옵션 노출 가능
- **서버 검증**: 초기엔 생략 가능 — 광고 SDK의 "시청 완료" 콜백을 신뢰하고 클라이언트에서 바로 `hintsLeft` 증가. 악용해봐야 힌트 하나 더 얻는 수준이라 리스크가 낮음. 어뷰징이 실제로 문제가 되면 그때 서버 카운팅 추가

## 2층 — 소액 일회성 결제

**무엇을 파는가**:
- **광고 제거 + 힌트 무제한 번들** (1회 구매) — 헤비 유저 대상 핵심 상품
- **캐릭터 스킨** — `Character.tsx`가 이미 `color` prop을 받도록 되어 있어 구현 난이도가 낮다. 개별 판매 또는 3종 묶음

**결제 수단**: 토스페이먼츠 — 카카오페이/네이버페이/카드를 한 번에 지원하고 한국어 문서가 잘 되어 있어 국내 유저 대상으로는 가장 마찰이 적다. (사업자 등록 요건은 실제 신청 전에 토스 쪽에서 개인/개인사업자 기준으로 직접 확인 필요)

- **플로우**: 상점 화면(일시정지 메뉴 또는 카테고리 선택 화면에 진입점) → 상품 선택 → 토스페이먼츠 위젯 → 결제 승인 웹훅을 백엔드가 수신 → `entitlements`에 기록 → 클라이언트가 재조회해서 즉시 반영
- **서버 필수**: 결제 승인은 반드시 서버(웹훅)에서 확정해야 한다 — 클라이언트가 "결제 성공"이라고 우기는 걸 그대로 믿으면 안 됨

## 3층 — 콘텐츠 확장 판매 (장기 매출 동력)

**무엇을 파는가**: 지금 5개 카테고리는 전부 무료로 유지하고, **앞으로 추가하는 신규 테마 카테고리**를 유료로 판매.

- 콘텐츠 제작 파이프라인은 이미 갖춰져 있음 (`tools/region-pools/*.json` + `generate-category-stages.ts`로 카테고리당 30스테이지 생성) — 실제 제작 비용은 새 사진 소스 확보 + 후보 영역 큐레이션 시간 정도
- **데이터 모델**: `client/src/data/categories.ts`의 `Category`에 `free: boolean` (또는 `priceKrw?: number`) 필드 추가
- **UX**: `CategorySelect` 카드에 잠긴 유료 카테고리는 가격 배지 + 자물쇠 표시, 클릭 시 결제 플로우로 연결. 구매 완료된 카테고리는 `entitlements` 조회로 잠금 해제
- 이 층은 2층의 결제 인프라(토스페이먼츠 + 웹훅 + entitlements)를 그대로 재사용하므로, 2층이 먼저 만들어져 있어야 자연스럽게 이어진다

## 제안 로드맵

| 단계 | 내용 | 선행 조건 |
|---|---|---|
| Phase 0 | 최소 백엔드 골격 (Express+SQLite, 익명 플레이어 ID, 스키마) | 없음 |
| Phase 1 | 보상형 광고 (힌트 +1) | 없음 — Phase 0과 병행 가능 |
| Phase 2 | 상점 UI + 토스페이먼츠 (광고제거+힌트팩, 스킨) | Phase 0 |
| Phase 3 | 신규 카테고리 유료화 | Phase 2 (결제 인프라 재사용) |

## 가격 제안 (초안, 추후 조정)

| 상품 | 가격 |
|---|---|
| 힌트 5개 팩 | ₩1,200 |
| 광고 제거 + 힌트 무제한 (1회) | ₩3,900 |
| 캐릭터 스킨 1종 | ₩1,200 |
| 캐릭터 스킨 3종 묶음 | ₩2,900 |
| 신규 카테고리 1개 | ₩1,900 ~ ₩2,900 |

## 리스크 / 주의사항

- 기존 무료 유저 반발 방지: 이미 공개된 5개 카테고리는 절대 유료 전환하지 않는다
- 웹 기반 보상형 광고는 생태계가 작고 단가가 낮음 — 진지하게 광고 수익을 키우려면 결국 앱스토어 출시(Capacitor 등)를 검토해야 함
- 토스페이먼츠는 사업자 등록 여부에 따라 가입 가능 여부/정산 방식이 달라짐 — 실제 신청 전 확인 필요
- 결제/광고 로직은 전부 서버 검증이 전제이므로, Phase 0(백엔드)을 건너뛰고 2층·3층을 먼저 시작할 수 없음
